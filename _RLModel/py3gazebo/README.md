# py3gazebo subtree placeholder

This directory previously tracked the `py3gazebo` submodule.
Populate it with the desired sources (e.g. via `git subtree` or by copying
files) so the reinforcement-learning tooling can import `py3gazebo` without
needing an external checkout.
